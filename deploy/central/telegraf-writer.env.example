# Installed as /etc/monitoring/central/telegraf-writer.env (0600) by install-central.sh.
# Populated automatically after InfluxDB setup. Do not commit the installed file.

INFLUX_TOKEN=
INFLUX_ORG=monitoring
INFLUX_BUCKET=network
